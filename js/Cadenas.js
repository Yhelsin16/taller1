let nombre= prompt("Ingrese su nombre");
let apellido= prompt("Ingrese su apellido");

console.log (`${nombre.toUpperCase()} y tiene ${nombre.length}  caracteres, ${apellido.toLowerCase()} y tiene ${apellido.length} caracteres`);
console.log (`${nombre.charAt(0)}${apellido}:Este es su usuario`)

